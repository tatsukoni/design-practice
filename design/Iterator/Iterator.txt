# 概要
    - 要素の集まりを保有するオブジェクトの各要素に順番にアクセスする方法を提供するためのパターン

# 目的
    - オブジェクトにアクセスする順番を、決める必要がある場合
    - この順番を決める方法を、ユーザーが作成・決定できるような柔軟な設計にすることを叶える

# ユースケース
    - 「具体的な集約体」があるかどうかが、キモになる。
    - このとき、集合体に依存させないような設計にする。